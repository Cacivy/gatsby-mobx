import React from 'react'
import styled from 'styled-components'

const FooterContainer = styled.div`
  margin: 10px 0;
  text-align: center;
`

export default () => (
  <FooterContainer>&copy;2018 power by gatsby</FooterContainer>
)
